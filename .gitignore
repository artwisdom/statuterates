# Dependencies
node_modules/
**/node_modules/
.pnp
.pnp.js

# Python
venv/
.venv/
__pycache__/
*.pyc

# Environment / secrets
.env
.env.local
*.key
*.pem

# Raw fetch cache (regenerated by pipeline; not the source of truth)
data/cache/

# SQLite is the runtime source-of-truth but is a regenerable binary build artifact.
# Git tracks the diff-friendly JSON snapshots in data/exports/ instead (see DECISIONS.md).
data/db.sqlite
data/db.sqlite-wal
data/db.sqlite-shm

# Build output
site/dist/
machine/mcp-server/dist/
machine/mcp-server/build/

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
