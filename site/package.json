{
  "name": "data-moat-engine-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static reference site (human skin) generated from the dataset at build time.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test src/lib/*.test.mjs",
    "test:browser": "playwright test",
    "test:browser:release": "node ../machine/build-api.mjs && ADSENSE_CLIENT=ca-pub-0000000000000000 ADSENSE_SLOT=0000000000 SITE_URL=https://statuterates.com npm run build && EXPECT_ADSENSE_CLIENT=ca-pub-0000000000000000 EXPECT_ADSENSE_SLOT=0000000000 npm run test:browser",
    "verify-build": "node scripts/check-build.mjs"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "astro": "7.3.3"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  },
  "overrides": {
    "svgo": "4.1.0"
  }
}
