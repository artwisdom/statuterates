{
  "name": "data-moat-engine-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fetch + normalize + validate + load pipeline for the Data Moat Engine dataset (SQLite source of truth).",
  "scripts": {
    "fetch": "node run.mjs fetch",
    "build-db": "node run.mjs build",
    "validate": "node run.mjs validate",
    "export": "node run.mjs export",
    "all": "node run.mjs all",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "better-sqlite3": "12.11.1",
    "pdfjs-dist": "6.1.200"
  }
}
